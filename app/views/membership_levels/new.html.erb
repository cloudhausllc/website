<div class="row">
  <div class="col-md-5 col-md-offset-4">
    <h1>New Membership Level</h1>
  </div>
</div>

<%= form_for(@membership_level) do |f| %>
    <% if @membership_level.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@membership_level.errors.count, "error") %> prohibited this user from being saved:</h2>

          <ul>
            <% @membership_level.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="row">
      <div class="box">
        <div class="col-md-5 col-md-offset-4">
          <div class="field">
            <%= f.text_field :name, class: 'form-control', placeholder: 'Name' %>
          </div>
          <div class="field">
            <%= f.text_field :monthly_payment, class: 'form-control', placeholder: 'Monthly Payment' %>
          </div>
          <div class="actions">
            <%= f.submit 'Create', class: 'form-control' %>
          </div>
        </div>
      </div>
    </div>

<% end %>
