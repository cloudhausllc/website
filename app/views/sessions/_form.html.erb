<%= form_for(:session, url: login_path) do |f| %>
    <div class="row">
      <div class="box">
        <div class="col-md-4 col-md-offset-4">
          <div class="field">
            <%= f.text_field :email, class: 'form-control', placeholder: 'Email' %>
          </div>
          <div class="field">
            <%= f.password_field :password, class: 'form-control', placeholder: 'Password' %>
          </div>
          <%# TODO: Remember me %>
          <div class="actions">
            <div class="field">
              <%= f.submit 'Login', class: 'form-control' %>
            </div>
            <div class="field">
              <%= button_tag 'New User? Register', 'data-url' => new_user_path, 'data-action' => 'navigate', method: 'get', class: 'form-control' %>
            </div>
          </div>
        </div>
      </div>
    </div>
<% end %>