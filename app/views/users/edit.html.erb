<div class="col-md-offset-3">
  <h1>Profile</h1>
</div>
<%= render 'profile' %>

<div class="col-md-offset-3">
  <h1>Membership</h1>
</div>
<%= render 'subscription_plan' %>

<div class="col-md-offset-3">
  <h1>Payment Methods</h1>
</div>
<%= render 'payment_methods' %>
<%# We'll just hide this until it has a bit more testing... %>
<% if User.cu_is_admin? %>
    <div class="col-md-offset-3">
      <h1>Charges</h1>
    </div>
    <%= render 'charges' %>

    <div class="col-md-offset-3">
      <h1>Invoices</h1>
    </div>
    <%= render 'invoices' %>
<% end %>