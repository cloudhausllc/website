<%= form_for(@web_hook_stripe_event) do |f| %>
  <% if @web_hook_stripe_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@web_hook_stripe_event.errors.count, "error") %> prohibited this web_hook_stripe_event from being saved:</h2>

      <ul>
      <% @web_hook_stripe_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :livemode %><br>
    <%= f.check_box :livemode %>
  </div>
  <div class="field">
    <%= f.label :stripe_id %><br>
    <%= f.text_area :stripe_id %>
  </div>
  <div class="field">
    <%= f.label :object %><br>
    <%= f.text_area :object %>
  </div>
  <div class="field">
    <%= f.label :request %><br>
    <%= f.text_area :request %>
  </div>
  <div class="field">
    <%= f.label :api_version %><br>
    <%= f.datetime_select :api_version %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_field :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
